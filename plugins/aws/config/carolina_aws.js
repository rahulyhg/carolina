/**
 * Configuration for the carolina/plugins/aws plugin.
 */
exports = module.exports = {
  
  // which profile in ~/.aws/credentials to use
  profile: "default",
  
  // which AWS region to use be default
  region: "us-east-1"
};